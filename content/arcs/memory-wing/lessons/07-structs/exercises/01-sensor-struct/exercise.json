{
  "id": "memory-wing-07-ex01-sensor-struct",
  "order": 1,
  "title": "Build and Print a Sensor",
  "type": "write-program",
  "prompt": "Using the struct sensor below, declare one on the stack, take its address into a pointer, and use -> to set id to 3 and reading to 101.5. Print exactly: Sensor 3: 101.5",
  "starterCode": "#include <stdio.h>\n\nstruct sensor {\n    int id;\n    double reading;\n};\n\nint main(void) {\n    struct sensor s;\n    struct sensor *p = &s;\n    // set p->id and p->reading, then print\n    return 0;\n}\n",
  "gradingCriteria": ["stdout-match"],
  "baseXp": 35,
  "timeoutSeconds": 5,
  "memoryLimitMb": 64
}
